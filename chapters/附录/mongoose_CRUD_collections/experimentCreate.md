### this.ctx.model.Experiment == db.experiments

```javascript
ctx.model.Experiment.create({
                                        ...parts.field,     // grammar sugar
                                       "createdAt": new Date(),
                                     });
```

